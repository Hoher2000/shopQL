type Sellers {
  id: ID!
  name: String!
  deals: Int!
}

type Items {
  id: ID!
  name: String!
  in_stock: Int!
  seller: Seller!
}

type Childs {
  id: ID!
  name: String!
  items: [Items!]!
}

type Catalog {
  id: ID!
  name: String!
  childs: [Childs!]!
}

type AutogeneratedMainType {
  sellers: [Sellers!]!
  catalog: Catalog!
}
